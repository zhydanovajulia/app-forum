<%= form_for ([@theme, @post]), :html => {:multipart => true} do |f| %>
  <%= render 'shared/error_message', :object => @post %>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
